<h1>User's Cart</h1>

<p>Here is your current cart...</p>

<ul>
  <% @cart.each do | item | %>
    <li>
      <%= item.name %> <%= item.price %>
    </li>
  <% end %>
</ul>

<button>Pay</button>
<%= link_to "Keep Searching", instruments_path %>