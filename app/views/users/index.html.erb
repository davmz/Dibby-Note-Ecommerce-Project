<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<ul>
  <% @users.each do | user | %>
    <li>
      <%= user.first_name %>,
      <%= user.last_name %>,
      <%= user.address %>,
      <%= user.email %>,
      <%= user.province_id %>
    </li>
  <% end%>
</ul>
