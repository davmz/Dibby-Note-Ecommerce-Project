<nav class="navbar is-black level px-6 py-3" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <div class="navbar-item">
      <img src="https://t3.ftcdn.net/jpg/01/95/82/02/360_F_195820215_3qBs8o8cUenR6H9ZWIjnKe60IXSb1xjv.jpg" width="50" height="50">
    </div>

    <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to "DibbyNote", root_path, class: "navbar-item" %>

      <%= link_to "Categories", types_path, class: "navbar-item" %>

      <%= link_to "Instruments", instruments_path, class: "navbar-item" %>

      <div class="navbar-item has-dropdown is-hoverable">
        <a class="navbar-link">
          More
        </a>

        <div class="navbar-dropdown">
          <% dibby_pages.each do | page | %>
            <%= link_to page.title, permalink_path(page.permalink), class: "navbar-item" %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          <% if user_signed_in? %>
            Logged in as <strong><%= current_user.email %></strong>

            <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "button is-primary is-rounded" %>

            <%= link_to "Edit Account", edit_user_registration_path, class: "button is-link is-rounded" %>
          <% else %>
            <%= link_to "Sign Up", new_user_registration_path, class: "button is-primary is-rounded" %>

            <%= link_to "Sign In", new_user_session_path, class: "button is-link is-rounded" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>

<%= form_with(url: search_path, method: :get, local: true) do  %>
  <div class="field is-grouped">
    <div class="control is-expanded" style="width: 350px">
      <%= text_field_tag(:keywords, nil, class: "input is-rounded", placeholder: "Search for Instrument") %>
    </div>

    <div class="select mr-4">
      <%= collection_select :type, :id, Type.all.order("id ASC"), :id, :name, { :prompt => true } %>
    </div>

    <div class="control">
      <%= submit_tag("Search", class: "button is-primary") %>
    </div>
  </div>
<% end %>